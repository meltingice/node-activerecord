{
  "name": "activerecord",
  "description": "An ORM that supports multiple database systems (SQL/NoSQL) and ID generation middleware.",
  "version": "0.0.1",
  "devDependencies": {
    "coffee-script": ">= 1.3.1",
    "sqlite3": ">= 2.1.3"
  }
}